services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: sundry
      POSTGRES_PASSWORD: sundry
      POSTGRES_DB: sundry_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
